#pragma once

#include "network/sender.hpp"
#include "network/receiver.hpp"
#include <string>
#include <vector>

class IByteSender;
class IByteReceiver;

//! This is the class that gets initialised by the client on startup.
class Client {
};
